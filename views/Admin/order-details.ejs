<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details - JOY Hardware</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/Admin/order-details.css">
</head>
<body class="bg-gray-100">
    <!-- Header -->
    <header class="bg-blue-800 text-white p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">JOY Hardware Seller Portal</h1>
            <div class="flex items-center space-x-4">
                <span class="text-sm">Welcome, Admin</span>
                <button class="bg-red-500 hover:bg-red-600 px-3 py-1 rounded text-sm">Logout</button>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="bg-gray-200 p-2 shadow-sm">
        <div class="container mx-auto flex space-x-4">
            <a href="/seller" class="px-3 py-1 rounded hover:bg-gray-300">Dashboard</a>
            <a href="/seller/products" class="px-3 py-1 rounded hover:bg-gray-300">Products</a>
            <a href="/seller/product/new" class="px-3 py-1 rounded hover:bg-gray-300">Add Product</a>
            <a href="/seller/orders" class="px-3 py-1 rounded bg-blue-100 text-blue-800 font-medium">Orders</a>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mx-auto p-4">
        <div class="flex justify-between items-center mb-6">
            <div>
                <h2 class="text-2xl font-bold text-gray-800">Order Details</h2>
                <p class="text-gray-600">Order ID: <%= order.orderid %></p>
            </div>
            <div class="flex space-x-2">
                <form action="/seller/order/delete/<%= order.orderid %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this order?');">
                    <button type="submit" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md text-white text-sm font-medium">
                        Delete Order
                    </button>
                </form>
                <button onclick="updateOrderStatus('<%= order.orderid %>')" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md text-white text-sm font-medium">
                    <i class="fas fa-sync-alt mr-1"></i> Update Status
                </button>
                <button onclick="printOrder()" class="bg-gray-600 hover:bg-gray-700 px-4 py-2 rounded-md text-white text-sm font-medium">
                    <i class="fas fa-print mr-1"></i> Print
                </button>
            </div>
        </div>
        
        <div class="bg-white rounded-lg shadow-md overflow-hidden mb-6">
            <!-- Order Summary -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4 p-6 border-b">
                <div>
                    <p class="text-sm text-gray-500">Order Date</p>
                    <p class="font-medium text-gray-800">
                        <%= new Date(order.createdAt).toLocaleDateString() %>
                    </p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Payment Method</p>
                    <p class="font-medium text-gray-800 capitalize">
                        <%= order.paymentMethod %>
                    </p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Transaction ID</p>
                    <p class="font-medium text-gray-800">
                        <%= order.transactionId %>
                    </p>
                </div>
                <div>
                    <p class="text-sm text-gray-500">Status</p>
                    
                </div>
            </div>

            <!-- Customer Information -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 p-6 border-b">
                <div>
                    <h4 class="text-lg font-medium text-gray-700 mb-3">Customer Information</h4>
                    <div class="space-y-2">
                        <p>
                            <span class="text-gray-600">Name:</span>
                            <span class="font-medium"><%= order.firstname %> <%= order.lastname %></span>
                        </p>
                        <p>
                            <span class="text-gray-600">Email:</span>
                            <span class="font-medium"><%= order.email %></span>
                        </p>
                        <p>
                            <span class="text-gray-600">Phone:</span>
                            <span class="font-medium"><%= order.phone %></span>
                        </p>
                    </div>
                </div>
                <div>
                    <h4 class="text-lg font-medium text-gray-700 mb-3">Shipping Address</h4>
                    <div class="space-y-2">
                        <p class="font-medium"><%= order.address %></p>
                        <p><%= order.city %>, <%= order.state %></p>
                        <p><%= order.pincode %>, <%= order.country %></p>
                    </div>
                </div>
            </div>

            <!-- Order Items -->
            <div class="p-6">
                <h4 class="text-lg font-medium text-gray-700 mb-4">Order Items</h4>
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Product</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Price</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Qty</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total</th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            <% order.items.forEach(item => { %>
                                <tr>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <div class="flex items-center">
                                            <div class="flex-shrink-0 h-10 w-10">
                                                <img class="h-10 w-10 rounded-md object-cover" src="<%= item.image %>" alt="<%= item.name %>">
                                            </div>
                                            <div class="ml-4">
                                                <div class="text-sm font-medium text-gray-900"><%= item.name %></div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">₹<%= item.price %></td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500"><%= item.quantity %></td>
                                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">₹<%= item.total %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Order Totals -->
            <div class="bg-gray-50 px-6 py-4">
                <div class="flex justify-end">
                    <div class="w-full max-w-md space-y-2">
                        <div class="flex justify-between">
                            <span class="text-gray-600">Subtotal:</span>
                            <span class="font-medium">₹<%= order.subtotal %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Shipping:</span>
                            <span class="font-medium">₹<%= order.shipping || 0 %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Tax:</span>
                            <span class="font-medium">₹<%= order.tax || 0 %></span>
                        </div>
                        <div class="flex justify-between border-t border-gray-200 pt-2">
                            <span class="text-lg font-bold text-gray-800">Total:</span>
                            <span class="text-lg font-bold text-gray-800">₹<%= order.totalPrice %></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Order Notes -->
        <div class="bg-white rounded-lg shadow-md p-6 mb-6">
            <h4 class="text-lg font-medium text-gray-700 mb-3">Order Notes</h4>
            <textarea class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" rows="3" placeholder="Add internal notes about this order..."></textarea>
            <div class="flex justify-end mt-3">
                <button class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md text-white text-sm font-medium">
                    Save Notes
                </button>
            </div>
        </div>

        <!-- Order Timeline -->
        <div class="bg-white rounded-lg shadow-md p-6">
            <h4 class="text-lg font-medium text-gray-700 mb-4">Order Timeline</h4>
            <div class="space-y-4">
                <div class="flex items-start">
                    <div class="flex-shrink-0 bg-blue-500 rounded-full p-1 text-white mr-3 mt-1">
                        <i class="fas fa-shopping-cart text-xs"></i>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-900">Order placed</p>
                        <p class="text-sm text-gray-500">
                            <%= new Date(order.createdAt).toLocaleString() %>
                        </p>
                    </div>
                </div>
                <!-- Additional timeline items would go here -->
                <div class="flex items-start">
                    <div class="flex-shrink-0 bg-gray-300 rounded-full p-1 text-white mr-3 mt-1">
                        <i class="fas fa-truck text-xs"></i>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-900">Order shipped</p>
                        <p class="text-sm text-gray-500">Pending</p>
                    </div>
                </div>
                <div class="flex items-start">
                    <div class="flex-shrink-0 bg-gray-300 rounded-full p-1 text-white mr-3 mt-1">
                        <i class="fas fa-check-circle text-xs"></i>
                    </div>
                    <div>
                        <p class="text-sm font-medium text-gray-900">Order delivered</p>
                        <p class="text-sm text-gray-500">Pending</p>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Status Update Modal -->
    <div id="statusModal" class="fixed inset-0 bg-black bg-opacity-75 z-50 hidden items-center justify-center p-4">
        <div class="bg-white rounded-lg shadow-xl max-w-md w-full">
            <div class="p-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">Update Order Status</h3>
                <div class="space-y-3">
                    <div class="flex items-center">
                        <input id="status-processing" name="status" type="radio" value="Processing" class="h-4 w-4 text-blue-600 focus:ring-blue-500">
                        <label for="status-processing" class="ml-3 block text-sm font-medium text-gray-700">Processing</label>
                    </div>
                    <div class="flex items-center">
                        <input id="status-shipped" name="status" type="radio" value="Shipped" class="h-4 w-4 text-blue-600 focus:ring-blue-500">
                        <label for="status-shipped" class="ml-3 block text-sm font-medium text-gray-700">Shipped</label>
                    </div>
                    <div class="flex items-center">
                        <input id="status-delivered" name="status" type="radio" value="Delivered" class="h-4 w-4 text-blue-600 focus:ring-blue-500">
                        <label for="status-delivered" class="ml-3 block text-sm font-medium text-gray-700">Delivered</label>
                    </div>
                    <div class="flex items-center">
                        <input id="status-cancelled" name="status" type="radio" value="Cancelled" class="h-4 w-4 text-blue-600 focus:ring-blue-500">
                        <label for="status-cancelled" class="ml-3 block text-sm font-medium text-gray-700">Cancelled</label>
                    </div>
                </div>
                <div class="mt-6 flex justify-end space-x-3">
                    <button type="button" onclick="closeStatusModal()" class="px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        Cancel
                    </button>
                    <button type="button" onclick="saveOrderStatus()" class="px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        Update Status
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="/script/Admin/order-details.js"></script>
</body>
</html>